{
  "name": "@block-agent/core",
  "version": "3.0.0",
  "description": "block-agent core runtime: Block tree, Operations + PolicyEngine, Renderer, App framework, providers, AgentRuntime. Zero runtime dependencies.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "$exports_note": "No '.' root export: src/index.ts is the demo boot (self-runs main()). Consumers import the classes/types they need via these area subpaths, e.g. '@block-agent/core/core/block.js'.",
  "exports": {
    "./core/*": "./src/core/*",
    "./app/*": "./src/app/*",
    "./apps/*": "./src/apps/*",
    "./provider/*": "./src/provider/*",
    "./runtime/*": "./src/runtime/*"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {},
  "$devDependencies_note": "Apps that moved out of core into the apps/ workspaces are devDeps here ONLY for core's integration/app tests (test/agent_identity.test.ts, projection_e2e.test.ts). They are dev-only, so core's RUNTIME closure (npm ls --omit=dev) stays empty — CI core-closure check unaffected.",
  "devDependencies": {
    "@block-agent/app-agent_identity": "*",
    "@block-agent/app-messages": "*",
    "@block-agent/app-tools": "*",
    "@block-agent/app-memory": "*",
    "@block-agent/app-task": "*",
    "@block-agent/app-stats": "*",
    "@block-agent/app-im_proxy": "*",
    "@block-agent/app-oa_proxy": "*",
    "@block-agent/app-task_proxy": "*"
  }
}
