{
  "name": "@block-agent/web",
  "version": "3.0.0",
  "description": "block-agent web inspector: a Vite + React SessionProtocol WS client. A conversation pane on the left and an animated, lazy-loaded context sidebar on the right (D3). Never imports core/cli — talks to the host only over WebSocket, sharing TYPES-only with @block-agent/protocol.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@block-agent/protocol": "*",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.0",
    "vite": "^5.4.0"
  }
}
