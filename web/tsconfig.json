{
  "$comment": "web/ is a browser bundle: DOM libs + react-jsx, bundler resolution (Vite owns module loading, not NodeNext). It NEVER imports core/cli — only TYPE-only frames from @block-agent/protocol — so this config is deliberately decoupled from the workspace's NodeNext tsconfig.base.json.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],

    "jsx": "react-jsx",
    "useDefineForClassFields": true,

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "exactOptionalPropertyTypes": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,

    "noEmit": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  },
  "include": ["src", "vite.config.ts"]
}
