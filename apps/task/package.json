{
  "name": "@block-agent/app-task",
  "version": "1.0.0",
  "description": "task BlockApp: local todo tracker; provides the task_count contract.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "$exports_note": "exports point at .ts SOURCE (no build). Importers use '@block-agent/app-task/manifest.js'.",
  "exports": {
    "./*": "./src/*"
  },
  "blockAgent": {
    "id": "task",
    "trust": "trusted",
    "host": "in-process",
    "default_enabled": true,
    "summary": "待办列表；提供 task_count 契约。",
    "provides": ["task_count"],
    "requires": null
  },
  "engines": { "node": ">=24" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@block-agent/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
