{
  "name": "@block-agent/app-messages",
  "version": "1.0.0",
  "description": "messages BlockApp: conversation history + auto-compression; provides the message_count contract.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "$exports_note": "exports point at .ts SOURCE (no build). Importers use '@block-agent/app-messages/manifest.js'; NodeNext resolves it via the workspace symlink.",
  "exports": {
    "./*": "./src/*"
  },
  "blockAgent": {
    "id": "messages",
    "trust": "trusted",
    "host": "in-process",
    "default_enabled": true,
    "summary": "对话历史与自动压缩。",
    "provides": ["message_count"],
    "requires": null
  },
  "engines": { "node": ">=24" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@block-agent/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
