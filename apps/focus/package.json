{
  "name": "@block-agent/app-focus",
  "version": "1.0.0",
  "description": "focus BlockApp: agent working-state / trajectory (goal + working-state + recent-action window). D5 P1.5a.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "$exports_note": "exports point at .ts SOURCE (no build). Importers use '@block-agent/app-focus/manifest.js'.",
  "exports": {
    "./*": "./src/*"
  },
  "blockAgent": {
    "id": "focus",
    "trust": "trusted",
    "host": "in-process",
    "default_enabled": true,
    "summary": "工作态与轨迹：目标块 + 工作态块 + 近期动作窗口（D5）。",
    "provides": [],
    "requires": null
  },
  "engines": { "node": ">=24" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@block-agent/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
