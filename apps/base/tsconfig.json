{
  "$comment": "Mirrors the app workspace pattern: NodeNext/strict/.js extensions. Single-direction dependency on @block-agent/core via workspace symlink + NodeNext resolution. Build-free dev (no tsc -b step).",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["src/**/*.ts", "test/**/*.ts", "vitest.config.ts"],
  "exclude": ["node_modules", "dist"]
}
